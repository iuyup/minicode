# MiniCode Security Model

MiniCode 是一个面向可信个人项目的轻量 Coding Agent。它通过本地策略、人工确认和审计记录缩小风险面，但不是容器、虚拟机或操作系统沙箱。

## 信任边界

- **模型不可信。** 模型输出、工具名、参数、补丁和终端文本都必须经过本地校验；提示词不是权限边界。
- **远程服务能看到发送给它的内容。** 远程 Profile 可能接收任务、目录与搜索结果、源码片段、Git 状态或差异、编辑参数，以及用户批准后运行项目检查所得的有限输出。
- **工作区默认被视为用户信任的代码。** `npm` 脚本和 Node 程序可以启动子进程、访问网络或修改文件。MiniCode 的确认面板不是对这些行为的沙箱。
- **仓库也可能影响 Git。** 专用 Git 工具会关闭 pager、外部 diff、textconv、filter、fsmonitor、按需抓取和可选锁，并预检本地配置；这仍不等于能够安全分析任意恶意仓库。

## 本地文件与远程数据

内置策略拒绝读取常见密钥、凭据目录和依赖目录。项目根目录可使用 `.minicodeignore` 进一步收紧 Agent 可读取的路径；自定义规则只能增加拒绝，不能解除内置保护。

使用远程 Profile 前，请确认工作区中没有不应发送给第三方的源码或业务数据。API Key 只从环境变量读取，不写入 Profile、仓库或 JSONL 审计；受控子进程使用经过筛选的环境变量。

## 模型连接

- 默认允许 HTTPS。
- 明文 HTTP 默认只允许 `localhost`、`127.0.0.1` 和 `[::1]`。
- 必须连接其他明文 HTTP 服务时，需显式设置 `MINICODE_ALLOW_INSECURE_HTTP=1`；这会暴露 API Key 和请求内容，不建议使用。
- HTTP 重定向被拒绝，避免凭据被自动带到另一个地址。
- 响应具有超时、正文大小、UTF-8 与结构校验；残缺响应不会被当成成功答案。

## 编辑与命令确认

- 截断的补丁预览不能进入 `APPLY`；长修改必须拆分为可完整展示的小补丁。
- 补丁只在 `oldText` 唯一匹配时执行，并在批准后复核文件内容与文件身份。
- 受控命令在 `RUN` 前冻结程序、参数与工作目录，并在启动前复核工作目录、关键入口文件及实际生效的 `package.json` 身份与内容；npm workspace 选择参数暂不开放。
- 所有确认与可控子进程均接受任务级取消信号。

这些检查只能缩小路径替换与 TOCTOU 窗口。纯 Node.js 无法跨平台原子保证父目录、Windows reparse point 和进程启动目标在检查后绝不变化。

## 使用建议

| 场景 | 建议 |
| --- | --- |
| 本人维护且可信的本地项目 | 支持；仍应查看每次 `APPLY` 和 `RUN` |
| 陌生 GitHub 项目 | 谨慎；先人工审查，在隔离环境中运行 |
| 企业生产仓库或含客户数据的环境 | 当前不建议直接使用远程模型 |
| 恶意代码分析、强隔离执行 | 不支持；请使用专用沙箱或虚拟机 |

MiniCode 不执行自动 commit、push、分支切换或 reset。Git 写操作由用户在 Agent 外手动完成。

## 报告安全问题

请优先使用仓库的 GitHub Security Advisory 私下报告。不要在公开 issue、日志或截图中附带真实 API Key、私有源码或客户数据。
